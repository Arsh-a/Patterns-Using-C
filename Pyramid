#include <stdio.h>

void Pyramid(int n)
{
    for(int i=0;i<n;i++)
    {
        for(int k=0;k<n-1-i;k++)
        {
            printf(" ");
        }
        for(int j=0;j<=i;j++)
        {
            printf("* ");
        }
        printf("\n");
    }
}


int main(){
    Pyramid(5);
}
